services:
  - type: web
    name: momybott
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn index:app --bind 0.0.0.0:$PORT







